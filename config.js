const host = 'www.ll9999.cn'

const config = {
  openIdUrl: `https://${host}/onLogin`,
  userRegisterUrl: `https://${host}/userRegister`,
  getUserVideoUrl: `https://${host}/getUserVideo`,
  uploadVideoUrl: `https://${host}/uploadVideo`,
  saveInteractiveVideo: `https://${host}/saveInteractiveVideo`,
  getUserInteractiveVideo: `https://${host}/getUserInteractiveVideo`
}

module.exports = config
